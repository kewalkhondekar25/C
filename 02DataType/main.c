#include <stdio.h>

int main(){
    
    // int, float(decimal), char
    // int age = 25;
    // float balance = 299.95;
    // char inital = "k";
    int i;
    char c;
    short s;
    printf("size of int: %d byte(s) \n", sizeof(i));
    printf("size of char: %d byte(s) \n", sizeof(c));
    printf("size of short: %d byte(s) \n ", sizeof(s));
    printf("size of float: %d byte(s) ", sizeof(float));




}